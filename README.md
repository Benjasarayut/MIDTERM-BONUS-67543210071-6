# MIDTERM-BONUS-67543210071-6

# 📚 Library Management System (Midterm Bonus Project)
---
📋 Project Information

Student Name: นาย เบญจศรายุทธ น้อยอุบล

Student ID: 67543210071-6

Course: ENGSE207 Software Architecture

---

## 🏛️ System Architecture

โปรเจกต์นี้มีการแยกส่วนการทำงานอย่างชัดเจน เพื่อความง่ายในการรักษาความปลอดภัยและการขยายระบบในอนาคต:

### 1. Infrastructure Setup
- **Client (Host Machine):** เว็บเบราว์เซอร์รัน Frontend ผ่านพอร์ตมาตรฐาน
- **Server (Ubuntu VM):** รัน Node.js Backend ที่ IP `192.168.56.101` พอร์ต `3000`
- **Database:** SQLite (Embedded Database) เก็บข้อมูลในไฟล์ `library.db` บนเครื่อง VM

### 2. Backend Layered Architecture (3-Layers)
เพื่อให้โค้ดเป็นระเบียบและง่ายต่อการ Debug ผมจึงแยกหน้าที่ของไฟล์ดังนี้:
- **Presentation Layer (Controllers):** จัดการ HTTP Request และส่ง Response กลับ (`bookController.js`)
- **Business Logic Layer (Services):** ประมวลผลตรรกะเบื้องหลังและเชื่อมโยงข้อมูล (`bookService.js`)
- **Data Access Layer (Repositories):** จัดการคำสั่ง SQL เพื่อคุยกับฐานข้อมูล (`bookRepository.js`)

---

## 🚀 Technology Stack

- **Frontend:** HTML5, CSS3 (Custom Design), Vanilla JavaScript
- **Backend:** Node.js, Express.js
- **Database:** SQLite3
- **DevOps/Tools:** Git, VirtualBox (Host-only Adapter), REST API

---

## ✨ Key Features

- **Full CRUD Operations:** สามารถเพิ่ม, ค้นหา, แก้ไข และลบข้อมูลหนังสือได้จริง
- **Real-time Statistics:** แสดงสถิติจำนวนหนังสือว่าง (Available) และที่ถูกยืม (Borrowed) โดยคำนวณจากฐานข้อมูล
- **Theme Support:** รองรับการเปลี่ยนธีม 3 รูปแบบ (Light, Dark, Green Mode)
- **Toast Notifications:** มีระบบแจ้งเตือนสถานะการทำงาน (Success/Error) ที่มุมจอ
- **CORS Enabled:** รองรับการสื่อสารข้ามเครื่องระหว่าง Host และ VM

---

## 🛠️ Installation & Setup

### Backend (On VM)
1. ติดตั้ง Dependencies:
    ```bash
    npm install
รันระบบ:

Bash

npm start
Frontend (On Host)
ตรวจสอบไฟล์ api.js ว่าระบุ BASE_URL ไปยัง IP ของ VM ถูกต้อง

เปิดไฟล์ index.html ผ่านเว็บเบราว์เซอร์ หรือใช้ http-server

🧪 API Documentation
สามารถดูวิธีการทดสอบ API ทั้งหมดได้ที่ไฟล์ API_TESTS.md

👤 Author
ชื่อ-นามสกุล: [ใส่ชื่อของมึงตรงนี้]

รหัสศึกษา: 67543210071-6


---

### 🚀 ขั้นตอนสุดท้ายก่อนนอน (ห้ามลืม!)
หลังจากสร้างไฟล์ `README.md` แล้ว อย่าลืมดันมันขึ้น Git เป็นครั้งสุดท้ายด้วยคำสั่งนี้ครับ:

```bash
git add README.md
git commit -m "Docs: Complete professional README with architecture details"
git push origin main
** Checklist ความสมบูรณ์:**

[x] README.md: สรุปโครงสร้างและวิธีรันงาน

[x] API_TESTS.md: สรุปวิธี Test ผ่าน curl

[x] Backend Code: แก้ไขบั๊ก updateBook เรียบร้อย

[x] Frontend Code: เชื่อมต่อ IP ของ VM ถูกต้อง
