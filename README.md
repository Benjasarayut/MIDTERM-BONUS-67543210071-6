# 📚 Library Management System (Bonus: Client-Server Edition)

ระบบจัดการข้อมูลห้องสมุดแบบ Full-Stack ที่ออกแบบตามสถาปัตยกรรม **Client-Server** และ **3-Layer Architecture** โดยจำลองสภาพแวดล้อมจริงผ่าน Virtual Machine

---

## 👤 Project Information
- **ชื่อ-นามสกุล:** นายเบญจศรายุทธ น้อยอุบล
- **รหัสประจำตัวนักศึกษา:** 67543210071-6
- **รายวิชา:** ENGSE207 Software Architecture

---

## 🏛️ System Architecture

โปรเจกต์นี้มีการแยกส่วนการทำงาน (Decoupling) เพื่อรองรับมาตรฐานความปลอดภัยและการจัดการระบบในอนาคต:

### 1. Infrastructure Setup
- **Client (Host Machine):** เว็บเบราว์เซอร์รัน Frontend ผ่านพอร์ตมาตรฐาน สื่อสารกับเซิร์ฟเวอร์ด้วย Fetch API
- **Server (Ubuntu VM):** รัน Node.js Backend บนสภาพแวดล้อม Ubuntu Server (IP: `192.168.56.101` Port: `3000`)
- **Network Management:** เชื่อมต่อผ่าน Host-only Adapter และเปิดรับการเชื่อมต่อแบบ External (Listen on 0.0.0.0)



### 2. Backend Layered Architecture (3-Layers)
มีการจัดการโครงสร้างโค้ดแบบ Separation of Concerns เพื่อความง่ายในการบำรุงรักษา:
- **Presentation Layer (Controllers):** ทำหน้าที่จัดการ HTTP Request/Response และ Endpoint Routing
- **Business Logic Layer (Services):** ประมวลผลตรรกะเบื้องหลัง (Logic) และเชื่อมโยงข้อมูลระหว่างเลเยอร์
- **Data Access Layer (Repositories):** จัดการคำสั่ง SQL และติดต่อกับฐานข้อมูล SQLite โดยตรง



---

## 🚀 Technology Stack

| ส่วนประกอบ | เทคโนโลยีที่เลือกใช้ |
| :--- | :--- |
| **Frontend** | HTML5, CSS3 (Custom Styles), Vanilla JavaScript |
| **Backend** | Node.js, Express.js |
| **Database** | SQLite3 (Embedded Relational Database) |
| **DevOps/Tools** | VirtualBox, Git, REST API, CORS Middleware |

---

## ✨ Key Features

- **Full CRUD Operations:** ระบบ เพิ่ม, ค้นหา, แก้ไข และลบข้อมูลหนังสือได้อย่างสมบูรณ์
- **Real-time Statistics:** หน้า Dashboard คำนวณสถิติ Available/Borrowed โดยตรงจากฐานข้อมูล
- **Multi-Theme Support:** รองรับการเปลี่ยนธีมหน้าเว็บ (Light, Dark, Green Mode)
- **Toast Notifications:** ระบบแจ้งเตือนสถานะการทำงาน (Success/Error) สำหรับ User Experience ที่ดี
- **CORS Enabled:** ปรับแต่งนโยบายความปลอดภัยเพื่อรองรับการสื่อสารข้ามเครื่อง (Cross-Origin)

---

## 🛠️ Installation & Setup

### Backend (บน Ubuntu VM)
1. ติดตั้ง Dependencies:
   ```bash
   npm install
เริ่มการทำงานของ Server:

Bash

npm start
Frontend (บนเครื่อง Host)
ตรวจสอบไฟล์ js/api.js เพื่อระบุ API_BASE_URL ไปยัง IP ของ VM ให้ถูกต้อง

เปิดไฟล์ index.html ผ่านเว็บเบราว์เซอร์ หรือใช้เครื่องมือเช่น http-server

🧪 Documentation & Testing
รายละเอียดการทดสอบ API ผ่านคำสั่ง curl: API_TESTS.md

รายละเอียดโครงสร้างสถาปัตยกรรมเพิ่มเติม: Architecture.md